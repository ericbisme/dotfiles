# Claude Code personal config

Versioned copies of the personal agents and rules that live in `~/.claude/agents/`
and `~/.claude/rules/`. Kept here for backup; `bootstrap.sh` does not link them,
so the live files are edited in place and copied back here by hand.
